# Market Coverage OS Agent Notes

Before making code changes, read `docs/project_journal.md`.

Keep changes small and preserve existing app behavior unless the task explicitly requires a broader change.

After each meaningful task or bug-fix session, update `docs/project_journal.md` so it reflects:

- What changed.
- What was validated.
- Which bugs are still open.
- Which bugs were fixed and are waiting for user confirmation.
- Which bugs the user confirmed are fixed.
